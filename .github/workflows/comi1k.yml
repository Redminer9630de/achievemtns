name: 1000 Commits

on:
  workflow_dispatch:

jobs:
  commit-burst:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: ⚙️ Git einrichten
        run: |
          git config user.name "Redminer9630de"
          git config user.email "redminer9360@gmail.com"

      - name: 🌀 Commit-Loop (1000x, alle 6s)
        run: |
          for i in {1..10}; do
            echo "Commit $i at $(date)" >> log.txt
            git add log.txt
            git commit -m "Auto Commit $i - $(date +%s)" || echo "Nichts zu committen"
            git push origin HEAD || echo "Push fehlgeschlagen"
            echo "[$i/1000] Commit abgeschlossen"
            sleep 6
          done

      - name: ✅ Fertig
        run: echo "🎉 Alle 1.000 Commits abgeschlossen!"
